# Data folder

`raw/` contains input parquet datasets.

`processed/` contains generated outputs from the notebooks or scripts.

For public repositories, do not commit proprietary Bloomberg data or large generated files unless distribution is allowed.
